---
const id = "terminal-donate";

const fullCopy = `Support channel: open.

This project is independent.
No ads. No token sales. No paywall.

If the work is useful, support keeps it moving:
• research time
• infrastructure + hosting
• writing + public documentation

Venmo person-to-person transfers typically avoid processing fees.
PayPal may apply standard processing fees.

Thank you. Seriously.
End of message.`;
---

<section class="rounded-2xl border border-white/10 bg-white/5 p-5 shadow-[0_0_0_1px_rgba(255,255,255,0.04)] backdrop-blur md:p-8">
  <div class="text-xs uppercase tracking-[0.2em] text-white/60">
    Support • Donate
  </div>

  <div class="mt-3 rounded-xl border border-white/10 bg-black/30 p-4 md:p-5">
    <pre class="m-0 overflow-x-auto text-sm leading-6 text-white/90 md:text-[15px]">
<code
  id={id}
  class="block min-h-[9rem] whitespace-pre-wrap"
  aria-live="polite"
  aria-label="Support terminal message"
></code><span class="terminal-cursor align-baseline">█</span>
    </pre>

    <!-- SEO + accessibility -->
    <div class="sr-only">
      <h2>Support message</h2>
      <pre>{fullCopy}</pre>
    </div>

    <noscript>
      <pre class="mt-4 whitespace-pre-wrap text-white/90">{fullCopy}</pre>
    </noscript>
  </div>

  <style>
    .terminal-cursor {
      display: inline-block;
      margin-left: 2px;
      opacity: 0.9;
      animation: blink 1.1s steps(1, end) infinite;
    }
    @keyframes blink {
      0%, 49% { opacity: 0.9; }
      50%, 100% { opacity: 0.0; }
    }
  </style>

  <script defer src="/js/terminalDonateClient.js"></script>
</section>

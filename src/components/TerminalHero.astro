---
const id = "terminal-hero";
import { terminalFullCopyText } from "../content/terminalScript";

---

<section class="rounded-2xl border border-white/10 bg-white/5 p-5 shadow-[0_0_0_1px_rgba(255,255,255,0.04)] backdrop-blur md:p-8">
  <div class="flex flex-col gap-6 md:flex-row md:items-start md:justify-between">
    <div class="min-w-0">
      <div class="text-xs uppercase tracking-[0.2em] text-white/60">
        Thesis Project • Public Launch
      </div>

      <div class="mt-3 rounded-xl border border-white/10 bg-black/30 p-4 md:p-5">
        <pre class="m-0 overflow-x-auto text-sm leading-6 text-white/90 md:text-[15px]">
<code
  id={id}
  class="block min-h-[8.5rem] whitespace-pre-wrap"
  aria-live="polite"
  aria-label="Terminal narrative"
></code><span class="terminal-cursor align-baseline">█</span>
        </pre>

        <!-- SEO + accessibility: full narrative exists in DOM -->
        <div class="sr-only">
          <h2>ThesisWeb narrative</h2>
          <pre>{terminalFullCopyText}</pre>
        </div>

        <noscript>
          <pre class="mt-4 whitespace-pre-wrap text-white/90">
{terminalFullCopyText}
          </pre>
        </noscript>
      </div>

      <p class="mt-4 max-w-2xl text-base leading-7 text-white/80">
        THE is an energy-anchored monetary system designed for fairness, auditability, and long-term stability —
        so the value you earn can’t be silently diluted.
      </p>

      <div class="mt-5 flex flex-col gap-3 sm:flex-row">
        <a
          href="#launch"
          class="inline-flex items-center justify-center rounded-xl bg-cyan-400/15 px-4 py-2.5 text-sm font-semibold text-cyan-200 ring-1 ring-cyan-300/25 hover:bg-cyan-400/20"
        >
          Get Launch Access
        </a>
        <a
          href="#support"
          class="inline-flex items-center justify-center rounded-xl bg-white/5 px-4 py-2.5 text-sm font-semibold text-white/90 ring-1 ring-white/15 hover:bg-white/10"
        >
          Support the Build
        </a>
      </div>
    </div>

    <div class="w-full md:w-[22rem]">
      <div class="rounded-2xl border border-white/10 bg-white/5 p-5">
        <div class="text-sm font-semibold text-white/90">
          What you’re signing up for
        </div>
        <ul class="mt-3 space-y-2 text-sm text-white/75">
          <li>• Launch-day access packet</li>
          <li>• Timeline + release notes</li>
          <li>• Builder updates (optional)</li>
        </ul>
        <div class="mt-4 text-xs text-white/50">
          No spam. Unsubscribe anytime.
        </div>
      </div>
    </div>
  </div>

  <style>
    .terminal-cursor {
      display: inline-block;
      margin-left: 2px;
      opacity: 0.9;
      animation: blink 1.1s steps(1, end) infinite;
    }
    @keyframes blink {
      0%, 49% { opacity: 0.9; }
      50%, 100% { opacity: 0.0; }
    }
  </style>

<script defer src="/js/terminalHeroClient.js"></script>

</section>
